<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= form.label :name, style: "display: block; margin-left: -300px;" %>
    <%= form.text_field :title, class: "form-control", style: "background: none; color: #fff;"%>
  </div>

  <div class="form-group">
    <%= form.label :description, style: "display: block; margin-left: -260px;" %>
    <%= form.text_area :body, class: "form-control", style: "background: none; color: #fff;"%>
  </div>
  
  <%= form.number_field :user_id, id: :post_user_id, value: current_user, type: :hidden %>

<br>
  <div>
    <%= form.submit class: "btn btn-outline-light" %>  
  </div>
<% end %>
 
  
